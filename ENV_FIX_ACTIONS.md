# Действия для исправления Environment Variables

Пошаговая инструкция по исправлению переменных окружения в Vercel для корректной работы приложения.

## 🚀 Быстрый старт с G2A Sandbox

Для тестирования G2A интеграции используйте sandbox credentials:

```
G2A_API_URL=https://sandboxapi.g2a.com/v1
G2A_API_KEY=74026b3dc2c6db6a30a73e71cdb138b1e1b5eb7a97ced46689e2d28db1050875
G2A_API_HASH=qdaiciDiyMaTjxMt
G2A_ENV=sandbox
```

Подробная инструкция: [ENV_SANDBOX_SETUP.md](ENV_SANDBOX_SETUP.md)

## 🔴 Добавить Redis (рекомендуется)

**Для production рекомендуется использовать Redis** для idempotency, метрик и кеширования.

**Действия**:

1. В Vercel Dashboard → Ваш проект → Settings → Environment Variables
2. Добавьте переменную:
   - **Key**: `REDIS_GKEYS_REDIS_URL`
   - **Value**: `redis://default:S0uqFAzNdrWOFeWYcGbQFcpmu6E3jAmu@redis-16640.c15.us-east-1-4.ec2.cloud.redislabs.com:16640`
   - **Environment**: Production (и Preview/Development если нужно)
3. Нажмите **Save**
4. Передеплойте проект

**Альтернатива**: Можно использовать `REDIS_URL` вместо `REDIS_GKEYS_REDIS_URL` - код поддерживает оба имени. Если установлены обе переменные, `REDIS_GKEYS_REDIS_URL` имеет приоритет.

**Проверка после добавления**:
- После деплоя проверьте логи - должно быть `✅ Redis connected`
- Проверьте health endpoint - `redis: 'ok'` в ответе

## 🚨 Критичные действия (обязательно)

### 0. Исправить VITE_API_BASE_URL (если есть ошибки парсинга URL)

**Проблема**: Если в консоли браузера видны ошибки типа `"/auth/register" cannot be parsed as a URL against "gkeys2-deffgods-projects.vercel.app/api"`, это означает что `VITE_API_BASE_URL` установлен без протокола `https://`.

**Действия**:

1. Откройте Vercel Dashboard → Ваш проект → Settings → Environment Variables
2. Найдите переменную `VITE_API_BASE_URL`
3. Проверьте значение:
   - ❌ **Неправильно**: `gkeys2-deffgods-projects.vercel.app/api` (без `https://`)
   - ✅ **Правильно**: `https://gkeys2-deffgods-projects.vercel.app/api` (с `https://`)
4. Если протокол отсутствует:
   - Нажмите на переменную для редактирования
   - Добавьте `https://` в начало значения
   - Например: `gkeys2-deffgods-projects.vercel.app/api` → `https://gkeys2-deffgods-projects.vercel.app/api`
5. Нажмите **Save**
6. Передеплойте проект

**Проверка после исправления**:
- После деплоя проверьте консоль браузера - не должно быть ошибок "cannot be parsed as a URL"
- Попробуйте зарегистрироваться - должна работать без ошибок
- Попробуйте войти - должна работать без ошибок
- Проверьте загрузку игр - должны загружаться без ошибок

**Примечание**: Код автоматически исправляет отсутствие протокола (добавляет `https://`), но лучше установить правильно в Vercel.

## 🚨 Критичные действия (обязательно)

### 1. Переименовать JWT_EXPIRY → JWT_EXPIRES_IN

**Проблема**: Код использует `JWT_EXPIRES_IN`, а в Vercel установлена `JWT_EXPIRY`. Без этого изменения будет использоваться значение по умолчанию `7d` вместо вашего `24h`.

**Действия**:

1. Откройте Vercel Dashboard: https://vercel.com/dashboard
2. Выберите ваш проект `gkeys2`
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите переменную `JWT_EXPIRY`
5. Скопируйте её значение (скорее всего `24h`)
6. Нажмите **Add New** и создайте новую переменную:
   - **Key**: `JWT_EXPIRES_IN`
   - **Value**: вставьте скопированное значение (например, `24h`)
   - **Environment**: выберите те же окружения, что и у `JWT_EXPIRY` (обычно Production, Preview, Development)
7. Нажмите **Save**
8. **НЕ удаляйте** `JWT_EXPIRY` пока не проверите работу
9. Передеплойте проект (или дождитесь автоматического деплоя)

**Проверка после изменения**:
- После деплоя проверьте логи - не должно быть ошибок
- Попробуйте войти в систему - токены должны работать с временем жизни `24h`

---

### 2. Переименовать G2A_API_SECRET → G2A_API_HASH (рекомендуется)

**Проблема**: Код поддерживает оба имени, но `G2A_API_HASH` предпочтительнее. При использовании `G2A_API_SECRET` будет предупреждение в консоли.

**Действия**:

1. В том же разделе Environment Variables найдите `G2A_API_SECRET`
2. Скопируйте её значение (это ваш G2A API Hash)
3. Создайте новую переменную:
   - **Key**: `G2A_API_HASH`
   - **Value**: вставьте скопированное значение
   - **Environment**: выберите те же окружения, что и у `G2A_API_SECRET`
4. Нажмите **Save**
5. **НЕ удаляйте** `G2A_API_SECRET` пока не проверите работу
6. Передеплойте проект

**Проверка после изменения**:
- После деплоя проверьте логи - не должно быть предупреждений о deprecated переменной
- Если используется G2A интеграция, проверьте что она работает

**Альтернатива**: Можно оставить `G2A_API_SECRET` - код будет работать, но будет предупреждение в консоли.

---

## ⚠️ Опциональные действия (можно сделать позже)

### 3. Добавить JWT_REFRESH_EXPIRES_IN (опционально)

**Текущая ситуация**: Используется значение по умолчанию `30d`. Если хотите изменить, добавьте переменную.

**Действия** (если нужно изменить):

1. В Environment Variables нажмите **Add New**
2. Создайте переменную:
   - **Key**: `JWT_REFRESH_EXPIRES_IN`
   - **Value**: например, `30d` или `90d` (рекомендуется в 3-10 раз больше чем `JWT_EXPIRES_IN`)
   - **Environment**: Production (и другие если нужно)
3. Нажмите **Save**
4. Передеплойте проект

**Если не добавляете**: Будет использоваться значение по умолчанию `30d`, что обычно нормально.

---

### 4. Удалить неиспользуемые переменные (опционально)

Эти переменные **не используются** в коде, но могут использоваться другими инструментами. Удаляйте только если уверены, что они не нужны.

#### POSTGRES_URL

**Действия** (если уверены, что не используется):
1. Найдите `POSTGRES_URL` в Environment Variables
2. Убедитесь, что никакие другие инструменты (мониторинг, бэкапы) не используют её
3. Если не используется - удалите
4. Передеплойте проект

**Если не уверены**: Оставьте переменную, она не мешает работе.

#### PRISMA_DATABASE_URL

**Действия**: Аналогично `POSTGRES_URL` - удалите только если уверены, что не используется.

#### CLIENT_ID и CLIENT_SECRET

**Важно**: Эти переменные используются для **PayPal**, а не для G2A. 

**Если они используются для PayPal**:
- Оставьте их как есть
- Или переименуйте в `PAYPAL_CLIENT_ID` и `PAYPAL_CLIENT_SECRET` для ясности

**Если они не используются**:
- Можно удалить

---

## 📋 Чеклист действий

Выполните действия в следующем порядке:

### Критичные действия (если есть ошибки парсинга URL):

- [ ] **0. Исправить VITE_API_BASE_URL** (если есть ошибки "cannot be parsed as a URL")
  - [ ] Проверить значение `VITE_API_BASE_URL` в Vercel
  - [ ] Убедиться что URL начинается с `https://`
  - [ ] Если отсутствует - добавить `https://` в начало
  - [ ] Сохранить
  - [ ] Передеплоить проект
  - [ ] Проверить что ошибки исчезли в консоли браузера

### Рекомендуемые действия (для production):

- [ ] **0. Добавить Redis** (рекомендуется для production)
  - [ ] Добавить `REDIS_GKEYS_REDIS_URL` в Vercel Environment Variables
  - [ ] Значение: `redis://default:S0uqFAzNdrWOFeWYcGbQFcpmu6E3jAmu@redis-16640.c15.us-east-1-4.ec2.cloud.redislabs.com:16640`
  - [ ] Установить для Production окружения
  - [ ] Сохранить
  - [ ] Передеплоить проект
  - [ ] Проверить логи: должно быть `✅ Redis connected`

### Обязательные действия:

- [ ] **1. Переименовать JWT_EXPIRY → JWT_EXPIRES_IN**
  - [ ] Скопировать значение из `JWT_EXPIRY`
  - [ ] Создать `JWT_EXPIRES_IN` с тем же значением
  - [ ] Установить для нужных окружений
  - [ ] Сохранить
  - [ ] Передеплоить проект
  - [ ] Проверить работу (войти в систему)

- [ ] **2. Переименовать G2A_API_SECRET → G2A_API_HASH** (рекомендуется)
  - [ ] Скопировать значение из `G2A_API_SECRET`
  - [ ] Создать `G2A_API_HASH` с тем же значением
  - [ ] Установить для нужных окружений
  - [ ] Сохранить
  - [ ] Передеплоить проект
  - [ ] Проверить работу G2A интеграции (если используется)

### После проверки работы:

- [ ] **3. Удалить старые переменные** (после успешной проверки)
  - [ ] Удалить `JWT_EXPIRY` (после проверки что `JWT_EXPIRES_IN` работает)
  - [ ] Удалить `G2A_API_SECRET` (после проверки что `G2A_API_HASH` работает)
  - [ ] Передеплоить проект

### Опциональные действия:

- [ ] **4. Добавить JWT_REFRESH_EXPIRES_IN** (если нужно изменить значение по умолчанию)
- [ ] **5. Удалить неиспользуемые переменные** (POSTGRES_URL, PRISMA_DATABASE_URL, CLIENT_ID, CLIENT_SECRET - только если уверены что не используются)

---

## 🔍 Проверка после изменений

После выполнения всех действий проверьте:

### Критичные проверки:

- [ ] Приложение успешно деплоится без ошибок
- [ ] Нет ошибок в логах Vercel о missing environment variables
- [ ] Можно войти в систему (проверка JWT)
- [ ] JWT токены работают с правильным временем жизни (24h если установили)
- [ ] Redis подключен (если добавлен): `✅ Redis connected` в логах
- [ ] G2A интеграция работает (если используется):
  - [ ] Нет ошибок аутентификации G2A API
  - [ ] Продукты синхронизируются корректно

### Дополнительные проверки:

- [ ] Нет предупреждений в консоли о deprecated переменных
- [ ] Все API endpoints работают корректно
- [ ] Админ панель доступна и работает

---

## 📝 Пример итоговой конфигурации

После всех изменений у вас должны быть следующие переменные:

### Обязательные (критично):

```
VITE_API_BASE_URL=https://gkeys2.vercel.app/api
DATABASE_URL=postgres://...
DIRECT_URL=postgres://...
JWT_SECRET=...
JWT_REFRESH_SECRET=...
FRONTEND_URL=https://gkeys2.vercel.app
NODE_ENV=production
```

### Обновленные (после миграции):

```
JWT_EXPIRES_IN=24h          # ← переименовано из JWT_EXPIRY
G2A_API_HASH=...            # ← переименовано из G2A_API_SECRET (рекомендуется)
```

### G2A интеграция (если используется):

```
G2A_API_URL=https://api.g2a.com/integration-api/v1
G2A_API_KEY=...
G2A_API_HASH=...            # или G2A_API_SECRET (работает, но deprecated)
G2A_ENV=live               # или sandbox
```

### Опциональные (рекомендуется для production):

```
JWT_REFRESH_EXPIRES_IN=30d  # опционально, по умолчанию 30d
REDIS_GKEYS_REDIS_URL=redis://default:S0uqFAzNdrWOFeWYcGbQFcpmu6E3jAmu@redis-16640.c15.us-east-1-4.ec2.cloud.redislabs.com:16640  # рекомендуется для production
# или
REDIS_URL=redis://...       # альтернатива REDIS_GKEYS_REDIS_URL
G2A_TIMEOUT_MS=8000         # опционально
G2A_RETRY_MAX=2             # опционально
```

---

## 🆘 Если что-то пошло не так

### Быстрый откат:

1. Восстановите старые переменные:
   - Восстановите `JWT_EXPIRY` (если удалили)
   - Восстановите `G2A_API_SECRET` (если удалили)
2. Удалите новые переменные (`JWT_EXPIRES_IN`, `G2A_API_HASH`)
3. Передеплойте проект

### Полный откат:

1. Используйте скриншот переменных (если делали)
2. Восстановите все переменные в исходное состояние
3. Передеплойте проект

---

## 📞 Нужна помощь?

Если возникли проблемы:

1. Проверьте логи в Vercel Dashboard → Ваш проект → Deployments → выберите последний деплой → Logs
2. Проверьте что все переменные установлены для правильных окружений (Production/Preview/Development)
3. Убедитесь что значения переменных правильные (особенно URLs)
4. См. [ENV_MIGRATION_GUIDE.md](ENV_MIGRATION_GUIDE.md) для подробной информации

---

**Важно**: После каждого изменения переменных **обязательно передеплойте проект**, иначе изменения не применятся!

**Последнее обновление**: 2024-12-23

