# Spec Kit Integration Guide

Spec Kit успешно интегрирован в проект GKEYS Store! 🎉

## ✅ Что было сделано

1. **Установлен Specify CLI** через `uv tool install`
2. **Инициализирован Spec Kit** в текущем проекте с поддержкой Cursor
3. **Создана конституция проекта** в `.specify/memory/constitution.md`
4. **Настроены шаблоны** для спецификаций, планов и задач

## 📁 Структура Spec Kit

```
.specify/
├── memory/
│   └── constitution.md          # Принципы и правила проекта
├── scripts/
│   └── bash/                    # Скрипты для автоматизации
└── templates/                   # Шаблоны для документов
    ├── spec-template.md
    ├── plan-template.md
    ├── tasks-template.md
    └── checklist-template.md
```

## 🚀 Использование Spec Kit

### Доступные команды в Cursor

Теперь в Cursor доступны следующие slash-команды:

#### Основные команды

1. **`/speckit.constitution`** - Создать или обновить принципы проекта
   ```
   /speckit.constitution Обнови принципы с акцентом на производительность и UX
   ```

2. **`/speckit.specify`** - Создать спецификацию новой функции
   ```
   /speckit.specify Добавить систему уведомлений для пользователей о новых играх и скидках
   ```

3. **`/speckit.plan`** - Создать технический план реализации
   ```
   /speckit.plan Использовать React Context для управления уведомлениями, WebSocket для real-time обновлений
   ```

4. **`/speckit.tasks`** - Разбить план на конкретные задачи
   ```
   /speckit.tasks
   ```

5. **`/speckit.implement`** - Выполнить все задачи и реализовать функцию
   ```
   /speckit.implement
   ```

#### Дополнительные команды (опционально)

- **`/speckit.clarify`** - Уточнить неясные моменты в спецификации (перед планированием)
- **`/speckit.analyze`** - Проверить согласованность артефактов (после задач, перед реализацией)
- **`/speckit.checklist`** - Создать чеклист качества (после планирования)

## 📋 Пример рабочего процесса

### Шаг 1: Создание спецификации
```
/speckit.specify Добавить страницу сравнения игр. Пользователь может выбрать до 4 игр для сравнения по цене, жанру, платформе, рейтингу и описанию. Страница должна быть адаптивной и показывать сравнение в табличном виде.
```

### Шаг 2: Уточнение (опционально)
```
/speckit.clarify
```

### Шаг 3: Создание плана
```
/speckit.plan Использовать React компоненты, создать новую страницу /compare, использовать существующий API для получения данных об играх, использовать таблицу shadcn/ui для отображения сравнения
```

### Шаг 4: Генерация задач
```
/speckit.tasks
```

### Шаг 5: Реализация
```
/speckit.implement
```

## 🎯 Конституция проекта

Конституция проекта находится в `.specify/memory/constitution.md` и содержит:

- **Принципы разработки**: Type Safety, Component-Driven, Performance
- **Технологический стек**: React 19, TypeScript, Vite, Tailwind CSS
- **Стандарты качества**: Тестирование, Code Review, Performance
- **Требования безопасности**: Аутентификация, валидация, защита от XSS/CSRF

Конституция будет автоматически использоваться при создании спецификаций и планов.

## 🔧 Проверка установки

Проверить, что все инструменты установлены:

```bash
specify check
```

## 📚 Дополнительная информация

- [Официальная документация Spec Kit](https://github.com/github/spec-kit)
- [Spec-Driven Development Guide](https://github.com/github/spec-kit/blob/main/spec-driven.md)

## 💡 Советы

1. **Начинайте с конституции** - Убедитесь, что принципы проекта актуальны
2. **Будьте конкретны в спецификациях** - Описывайте ЧТО и ЗАЧЕМ, а не КАК
3. **Используйте уточнения** - `/speckit.clarify` помогает избежать переделок
4. **Проверяйте планы** - Убедитесь, что план соответствует конституции
5. **Тестируйте после реализации** - Spec Kit создает код, но тестирование - ваша ответственность

## 🎉 Готово к использованию!

Spec Kit полностью настроен и готов к использованию. Начните с создания спецификации для новой функции!
